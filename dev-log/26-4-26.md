Today i read about: TCP and HTTP in this book: [High Performance Browser Networking](https://hpbn.co/building-blocks-of-tcp/)
Mostly focusing on chapter 1, 2 and 3.

Learned about what actually is in the TCP-headers and the Flow Control mechanism of TCP, Recive window (rwnd), Congestion window size (cwnd) and how these help stop congestion over the network.

TCP fast open (TFO) is a method used in TCP that allows the transport of data to begin already in the ACK part of the three way handshake.

* TCP three-way handshake introduces a full roundtrip of latency.
* TCP slow-start is applied to every new connection.
* TCP flow and congestion control regulate throughput of all connections.
* TCP throughput is regulated by current congestion window size. 

Slow start 


